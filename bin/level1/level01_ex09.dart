main() { 
  var sidekick = "Prof. Polymer";
  // var message = "$sidekick in lowercase is ________";  <- ${sidekick.toLowerCase()}
  var message = "$sidekick in lowercase is ${sidekick.toLowerCase()}";
  print(message);
}
